{
  "name": "recursive",
  "version": "0.0.0",
  "description": "test deferred at each level of recursion",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spatialdev",
  "license": "ISC",
  "dependencies": {
    "q": "^1.4.1"
  }
}
